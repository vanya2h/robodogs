export const produceWorld = (size: number): IWorld => {
	const world: IWorld = {
		size,
		areas: [],
		decorations: [],
	}

	for(let i = 0; i < size; i++) {
		for(let j = 0; j < size; i++) {
			/** Write implementation */
		}
	}

	return world;
}